# typescript-eslint 8.x declares typescript <6.0.0 as peer dependency.
# TypeScript 6 works correctly with the current eslint configuration;
# the constraint is conservative upstream packaging, not a runtime issue.
legacy-peer-deps=true
